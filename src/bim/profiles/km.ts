import type { ProfileCapabilities } from "../types";

export const kmProfileCapabilities: ProfileCapabilities = {
  dataBrowser: false,
  drawings: false,
  dxf: false,
  issues: false,
  qaQc: false,
};
